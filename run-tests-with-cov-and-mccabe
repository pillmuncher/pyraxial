if [ "$1" == "" ]
 then
    python3 -m pytest --doctest-modules --cov=pyraxial --cov-append --mccabe tests/test_pyraxial.py
 else
    python3 -m pytest --doctest-modules --cov=pyraxial --cov-append --mccabe "$1"
 fi
